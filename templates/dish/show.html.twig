{% extends 'base.html.twig' %}

{% block title %}Dish{% endblock %}

{% block body %}
<div class="col-11 mx-auto">
    <h3 class="quotes p-4 shadow">Plat</h3>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dish.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ dish.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ dish.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dish_index') }}">retour à la liste</a>

    <a href="{{ path('app_dish_edit', {'id': dish.id}) }}">editer</a>

    {{ include('dish/_delete_form.html.twig') }}
</div>
{% endblock %}
