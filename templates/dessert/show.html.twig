{% extends 'base.html.twig' %}

{% block title %}Dessert{% endblock %}

{% block body %}
<div class="col-11 mx-auto">
    <h3 class="quotes p-4 shadow">Dessert</h3>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dessert.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ dessert.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ dessert.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dessert_index') }}">retour à la liste</a>

    <a href="{{ path('app_dessert_edit', {'id': dessert.id}) }}">editer</a>

    {{ include('dessert/_delete_form.html.twig') }}
</div>
{% endblock %}
